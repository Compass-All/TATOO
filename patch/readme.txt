all.txt is to patch let debug v3 lowrisc to run in kc705 and has the memory tagging attribute and add a coprocessor to lowrisc processor.
bbl.patch is to patch the bootloader
gnu.patch is to patch the gnu( commit id ) so that the gnu can compile the instruction.
